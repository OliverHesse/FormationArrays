How to handle networking

2 main methods

method 1:
    sync the whole formation instance, this would be done every time the instance requests it.

method 2:
    add a FormationClientInstance that holds(by default) only runtime data, this way during syncs we only need to provide runtime data
    we can then also provide extra info (like is active or is destroyed)

I tend towards the 2nd method. but, it would require an extra generic for our formation.
the only thing we really want handled on the client is rendering, so i could rename it to FormationRenderer?

either way i do not think it needs to be part of the api? since the only time we decode it is from the formation anyway, and the formation
only needs to return a raw FormationInstance anyway

because the formation itself will never receive the client ver we dont need to include it as a generic

so this can be an implementation detail.

the main thing then is setting up the ClientDimensionNodemanager

It can be miles simpler than our old one, only needing to store the formation instances, some id we can associate over the network and
run a ticker.




it could also handle syncing. the tick method on the instances returns true/false
with true meaning isDirty
we then add it to the dirty formations and setup a patch
